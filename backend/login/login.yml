login:
  handler: login/handler.login
  events:
    - http:
      method: GET
      path: /login/{username}
      request:
        parameters:
          paths:
            username: true
      cors: true
    - http:
      method: POST
      path: /login
      cors: true